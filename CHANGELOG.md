# 变更日志

## 1.2.1 — 2026-09-23

### 修复

- 按 `Shift` 状态区分 `M` 快捷键：普通 `M` 只升满选中目标，`Shift+M` 才升满全部建筑、床和铁门；快捷键不再打断输入框编辑。
- 收紧 `Cmd` 命令字段和值校验；远程命令仅接受主机登记的已认证 peer，拒绝重复/倒序序号和远程跳波命令。
- 限制局内抽奖为 1 抽或 10 连，拒绝负数、超量、无效币种及未知技能，避免异常输入造成资源错误或运行时异常。
- 修正建筑详情中显示持续耗电、但游戏实际不会扣除运行电力的问题。
- 修正抽奖、电力与存档校验的文档说明；存档系统只做结构校验，不计算校验和。

### 构建

- 统一项目版本到 `1.2.1`；构建会把 `VERSION` 写入 HTML 元数据。
- 重新生成 `index.html` 和 `tangping.html`，使其与当前源码一致。
