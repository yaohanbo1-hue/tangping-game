# 联机可行性评估

> 结论先说：**能做，推荐「房主权威 + 状态同步」的合作 PvE**。
> 建议不要走锁步（lockstep）—— 在这个技术栈上它比看起来脆弱得多。
> 本轮已经把三件必要的地基打好了（完整状态快照 / 命令层 / 固定步长），见文末。

---

## 一、这个项目适合什么形式的联机

| 形式 | 可行性 | 说明 |
|---|---|---|
| **合作 PvE（2~4 人一起守三路）** | ✅ 推荐 | 玩法天然适配：三路走廊正好可以一人一路；建筑与金币共用，需要共同决策 |
| 对战 PvP（互派梦魇） | 🔶 可做 | 需要重做波次生成与平衡，工作量约等于一个新玩法 |
| 异步排行榜 / 幽灵回放 | ✅ 最省事 | 只需要「存档 + 命令日志」上传，本轮已经具备 |
| 同屏本地双人 | ⚠️ 不推荐 | 当前是固定 1280×720 画布 + 鼠标单点，分屏要重做整套输入与布局 |

---

## 二、为什么不推荐锁步（lockstep）

锁步（所有人只同步输入，各自跑一份完全相同的模拟）在 RTS 里很香，但要求**逐位确定性**，本项目有三处硬伤：

1. **JS 浮点跨浏览器不可靠**：`Math.sin/cos/hypot/pow` 在不同引擎（V8 / SpiderMonkey / JavaScriptCore）上不保证逐位一致。本作的伤害计算里到处是 `Math.pow`、`Math.hypot`，跑几百波后必然分叉。
2. **随机数没有种子**：全项目约 200 处 `Math.random()`（伤害暴击、掉落、词缀、生态事件…）。改成种子 RNG 不难，但**渲染层也在消耗同一个流**（粒子、拖尾都用 `rnd()`），会让画面帧率影响模拟结果。
3. **浮点误差会累积**：`dt` 累加、位置积分、护盾取整……一旦分叉，两边看到的世界就不一样了，而修正它比换方案更贵。

**状态同步没有这些问题**：只有房主跑模拟，其他人接收结果。

---

## 三、推荐的架构

```
         ┌──────────── 房主 (Host) ────────────┐
         │  完整游戏模拟（就是现在的 step()）    │
         │  校验远端命令 → 执行 → 产出快照       │
         └───────┬───────────────────▲──────────┘
    10Hz 快照     │                   │  命令（意图）
                 ▼                   │
   ┌─────────────────────────────────┴────────────┐
   │            客户端 (Client)                    │
   │  接收快照 → 覆盖本地状态 → 正常 render()      │
   │  玩家操作 → Cmd.dispatch → 发给房主（不改状态）│
   └──────────────────────────────────────────────┘
```

关键点：**渲染层不用改**。`render()` 是纯读 `G` 的，客户端只要把收到的快照写回 `G`，画面自然就对了。

### 传输层选择

| 方案 | 成本 | 适用 |
|---|---|---|
| **WebRTC DataChannel + 房间码**（PeerJS / 自建信令） | 低 | 2~4 人合作，延迟最好（P2P 直连） |
| WebSocket 权威服务器 | 中 | 需要防作弊、观战、匹配 |
| CloudStudio 起一个 Node 房间服务 | 低 | 本项目已部署在 CloudStudio，天然可用 |

起步建议：**PeerJS 房间码**（一人建房，其他人输 4 位码），信令服务器是唯一服务端依赖。

---

## 四、同步什么（快照子集设计）

全量 `SaveSystem.capture()` 约 5KB，10Hz 广播 = 50KB/s，P2P 完全扛得住。但可以更省：

| 字段 | 频率 | 说明 |
|---|---|---|
| `enemies[]` | 每帧（10Hz） | 只发 `id/x/y/hp/shield/state/phase/状态标记`，约 40 字节/只 |
| `bullets[]` | 可省略 | 客户端本地按「塔 → 目标」自行插值画弹道；或发最近的 20 发 |
| `buildings[]` | 变更时 | 事件驱动（建造/升级/转职/出售），不每帧发 |
| `doors/bed` | 变更时 | 同上 |
| `gold/power/souls` | 1Hz | 数值不需要高频 |
| `wave/state/prepTimer` | 变更时 | |
| `effects/parts/texts` | **不同步** | 纯表现，各自本地生成 |
| `rand` 相关 | **不同步** | 各自本地随机，不影响玩法 |

估算：4 人房、场上 40 只敌人 → 约 2KB/快照 × 10Hz = **20KB/s**，很轻松。

---

## 五、待做阶段与工作量

| 阶段 | 内容 | 估时 |
|---|---|---|
| **A. 地基** | 完整状态快照、命令层、固定步长 | ✅ **本轮已完成** |
| **B. 连接** | PeerJS 房间码、握手、房主/客机角色、心跳 | 2~3 天 |
| **C. 同步** | 快照广播 + 客户端插值渲染 + 命令转发与校验 | 3~5 天 |
| **D. 体验** | 大厅 UI、玩家颜色标识、断线重连、聊天/标记 | 2~3 天 |
| **E. 加固** | 房主侧校验（防改金币/秒建）、时间缩放与暂停语义、版本不一致拒绝 | 2 天 |

合计约 **2 周**做出「能和朋友稳定联机打完一局」的版本。

---

## 六、真正的难点（按难度排序）

1. **命令校验**：客机发 `{t:'build',k:'laser',c:3,r:2}`，房主必须自己再判一次金币、格子占用、是否在备战期。否则改个 JSON 就能白嫖建筑。`Cmd.exec` 已经是唯一入口，加校验只需要在这一处。
2. **时间控制语义**：现在 `paused` / `timeScale` 是本地全局。联机后「暂停」必须是全员投票或只有房主能按，否则有人按暂停全世界卡住。
3. **断线重连**：房主掉线 = 全房结束（P2P 的固有代价）；客机掉线可以靠"发全量快照"恢复。
4. **不同步带来的一致性问题**：客户端看到的是 100ms 前的世界，会出现"我明明看到怪在门口，房主说已经死了"。要靠插值 + 客户端预测缓解（本项目暂不需要，因为不是操作即时反馈类游戏）。
5. **管理员模式 / 跳波**：这类后门命令应只在房主本地生效，不广播。

---

## 七、本轮已经打好的地基

### 1. 完整状态快照 —— `save.js` 的 `SaveSystem.capture() / restore()`
这就是状态同步的序列化层。22 个顶层字段全部可 JSON 化（建筑用格子坐标、门用 lane、床单独一段），
实测**零差异往返**，并且带结构校验、版本迁移、坏档不崩。联机时把它拆成"高频子集"即可。

### 2. 命令层 —— `net.js` 的 `Cmd`
所有会改 `G` 的玩家操作已经**全部**改成经 `Cmd.dispatch()` 派发（建筑/升级/转职/出售/技能/召唤/符文/摇奖/跳波，共 31 处调用点）。
命令是纯数据、只用可序列化引用：

```js
{ t:'build',   k:'laser', c:3, r:2 }
{ t:'upgrade', c:3, r:2 }        // 或 { what:'bed' } / { what:'door', lane:1 }
{ t:'skill',   k:'meteor' }
{ t:'rune',    op:'socket', c:3, r:2, slot:0, runeId:'r12' }
```

`Cmd.dispatch` 的单机路径是"立即 exec"，所以你**感觉不到任何变化**；一旦把 `Net.enabled = true` 且角色是客机，
它会自动改为"只发送"。`Cmd.log` 还会记录命令序列，配合固定步长可以复现 bug。

### 3. 固定时间步长 —— `main.js` 的累加器
模拟恒定按 `1/60` 秒推进，帧率只影响画面平滑度，不再影响模拟结果。这是联机与复现的共同前提。
顺带修好了「低帧率时游戏变慢」的老问题（现在会追帧，上限 0.25 秒）。

### 4. 接缝 —— `Net`（占位）
```js
const Net = { enabled: false, isHost: true, send(cmd){ /* 阶段B 接 DataChannel */ }, onRemote(cmd){ Cmd.exec(cmd); } };
```
阶段 B 只需要实现 `send` 并在收到远端消息时调 `Net.onRemote`，其余代码不用动。

---

## 八、如果只想先迈一小步

**异步对战 / 排行榜**（约 1 天）：把 `SaveSystem.capture()` 的摘要 + 结算数据上传到 CloudStudio 的一个接口，
就能做出「全服最高波次榜」「同一存档种子比谁撑得久」。不需要任何实时同步，风险最低。
